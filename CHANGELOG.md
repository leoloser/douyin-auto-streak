# 维护与更新日志

本文件记录项目的主要功能变更、问题修复和维护事项。公开日志只描述通用行为，不记录真实好友昵称、截图、Cookie、浏览器配置目录里的私有数据或 `config.local.json` 内容。

## 2026-09-10

### 修复

- 修复项目迁移后 Windows 任务计划仍指向旧目录、固定时间启动立即退出的问题。
- 任务计划 Action 已更新为项目当前所在目录，并明确使用
  `--desktop-mode isolated`。
- 修复隔离桌面模式只移动终端、自动化 Edge 却创建在主桌面的问题：
  - 工作进程会持续识别使用专用用户目录的 Edge 窗口。
  - Edge 创建后会被移动到临时桌面，任务运行期间若被移走也会自动校正。
  - 避免用户切换到临时桌面后，Edge 留在其他桌面导致坐标点击频繁重试。

### 新增

- 新增 `setup_scheduled_task.ps1`：
  - 根据脚本自身所在目录创建或更新任务计划，不再要求手工写死项目路径。
  - 默认配置每天 `01:00`、`14:00`、`23:00` 运行。
  - 更新已有任务时优先沿用原 Python 可执行文件。
- 启动器新增 `--dry-run`，可验证隔离桌面和任务计划完整流程而不发送消息。

### 验证

- 在任务计划中进行隔离桌面 dry-run：
  - 外层任务返回码为 `0`。
  - 隔离桌面工作进程成功启动并完成全部目标会话检查。
  - 测试过程未发送消息，运行日志和 `last_run_summary.json` 均正常写入。
- 验证自动化 Edge 窗口位于名为“抖音续火花”的临时桌面。
- 将测试 Edge 强制移动到主桌面后，窗口监控能在数秒内将其自动移回临时桌面。
- dry-run 结束后，自动化 Edge、工作进程和临时桌面均正常清理。

## 2026-09-09

### 新增

- 对照 GitHub 开源同类项目补充无人值守运行能力：
  - `config.local.json` 支持 `message_candidates` 随机消息池。
  - 支持通过本地配置调整日志目录、失败截图目录、运行摘要路径和等待时间。
  - 每次运行结束写入 `last_run_summary.json`，便于任务计划场景排查。
  - 失败截图默认收拢到 `screenshots/`，运行日志默认写入 `logs/`。
- 定时任务支持隔离桌面运行：
  - `launch_douyin_keep_streak.py --desktop-mode isolated` 会创建临时 Windows 虚拟桌面。
  - 启动器会在临时桌面里再起一个脚本进程，让脚本本体和自动化 Edge 一起在那边运行。
  - 脚本结束后会关闭本次创建的临时虚拟桌面。
  - `launch_douyin_keep_streak.py --desktop-mode current` 明确保持在当前桌面运行，便于人工测试。
  - 兼容旧参数 `--isolated-desktop`。
- 增加可选依赖 `pyvda`，用于 Windows 虚拟桌面控制。
- README 补充隔离桌面、任务计划参数、随机消息池和运行排查产物说明。

### 修复

- 修复跨午夜聊天记录判断问题：
  - 当聊天记录中先出现“昨天 23:xx”，之后出现“01:xx”时，脚本会把后面的凌晨时间正确识别为今天。
  - 避免 01:00 已经发送过消息后，14:00 再次重复发送。
- 放宽抖音网页端居中时间标签的宽度判断：
  - 抖音会把 `01:00` 这类短时间放在一个较宽的居中容器里。
  - 现在不再因为时间标签容器较宽而漏识别。

### 验证

- Python 编译检查通过：
  - `python -m py_compile douyin_keep_streak.py launch_douyin_keep_streak.py`
- 浏览器注入 JavaScript 语法检查通过。
- 隔离桌面烟测通过：
  - 能创建临时虚拟桌面。
  - 能移动测试 Edge 窗口。
  - 能返回原桌面并关闭临时虚拟桌面。
- Dry-run 验证通过：
  - 对已经在今天发送过脚本消息的会话，能够识别并跳过。

## 2026-09-06

### 修复

- 改进“今天是否已续火花”的判断：
  - 不再只依据左侧聊天列表判断是否跳过。
  - 会打开聊天框后确认今天是否存在我方发出的消息。
  - 避免“对方今天发来消息，但我方还没发送”时被误判为已续火花。

### 验证

- Dry-run 验证通过：
  - 对方消息不会再被当成我方续火花记录。
  - 已存在我方今日消息的会话仍会跳过。

## 2026-09-04

### 修复

- 收紧今天活动检测规则：
  - 只把独立的短时间标记识别为今天，例如 `刚刚`、`5分钟前`、`今天 12:00`、`12:30`。
  - 不再把消息正文里的“刚刚”等字样误判为时间戳。
- 左侧列表检测旧日期优先：
  - 如果列表行出现 `昨天`、`前天`、星期或旧日期，优先判定为非今日记录。

### 验证

- 文本规则自测通过：
  - `昨天 00:53` 加正文内容不会被误判为今天。
  - `刚刚`、`几分钟前` 等列表时间仍能识别为今天动态。
- Python 编译检查通过。
- 浏览器注入 JavaScript 语法检查通过。

## 2026-08-28

### 新增

- 初始公开版本：
  - 支持 Microsoft Edge 自动化打开抖音网页版私信页。
  - 支持配置目标好友列表。
  - 支持逐个搜索并打开目标聊天框。
  - 支持发送固定消息 `1`。
  - 支持基础日志输出。
  - 支持操作间隔，避免高速循环操作。
- 增加本地私有配置：
  - `config.local.json` 用于保存真实好友列表和本地运行配置。
  - `config.local.json` 已加入 `.gitignore`，不会提交到公开仓库。
- 增加项目说明文件：
  - `README.md`
  - `.gitignore`
  - `requirements.txt`
  - `config.example.json`

### 维护

- 使用独立 Edge 用户数据目录保存抖音登录状态。
- 启动前会清理残留的自动化专用 Edge。
- 结束时只关闭本次脚本启动的自动化 Edge，不关闭普通 Edge。
- Windows 任务计划配置为每天 `01:00`、`14:00`、`23:00` 自动运行。

## 后续维护约定

- 每次功能更新或 bug 修复后，在本文件顶部追加一条记录。
- 公开日志不写真实好友名单、个人账号信息、截图文件名或本地私有配置内容。
- 与自动化平台相关的规则变化，需要重新 dry-run 验证后再启用真实发送。

## 风险提示

自动化操作第三方网页存在账号风控、限流甚至封号风险。本项目仅作技术演示，使用者需自行承担相关风险。
